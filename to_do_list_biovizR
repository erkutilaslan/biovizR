- finish the documentation on github page.

- add type parameter to all functions. this parameter indicates what kind of output is the
  data is generated from. for example selecting deseq2 or edger enables proper data wrangling
  to generate plot.

- add automatic mapping to HGNC for all identifiers inside functions.

- heatmap should accept a list rather than a List file.

- make sure both excel and csv file formats are ssupported as input

- add custom leveling to ma plot function. this can be done by manually changing the pvalue inside if it is lower than the FDR!!

- add cell cycle and proliferation analysis and visualization from flow cytometry data

- add dual luciferase results analysis

- solve windows compatibility issues: violin plot x_lab y_lab
